import AppShell from "../components/AppShell";

export default function Dashboard() {
  return (
    <AppShell>
      <p>Welcome. Lessons, the tool launcher, docs, and the report builder arrive in later phases.</p>
    </AppShell>
  );
}
