"""AI Red Team Lab — read-only observation library.

The tools in this package gather evidence only. They never modify a
target, never exploit a weakness, and never egress beyond the defined
assessment scope (see CLAUDE.md §2 — Hard Safety Constraints).
"""

__version__ = "0.1.0"
