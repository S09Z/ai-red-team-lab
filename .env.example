# Inclusive DDMMYYYY:DDMMYYYY window during which the scheduled auto-merge
# workflow (.github/workflows/auto-merge.yml) is allowed to run.
# Mirror this value into the GitHub Actions repository variable
# AUTO_MERGE_RANGE for CI to consume (the local .env is gitignored).
AUTO_MERGE_RANGE=01072026:30072026
